<div class="container text-center">
  <div class="text-center m-3 pt-3 pb-3">
    <h1>You've made a new Mooking!</h1>
  </div>
  <ul>
    <div class="card">
      <p>Your Cow is <%= @booking.cow.name %> 🐄</p>
      <% length=(@booking.end_date - @booking.start_date).to_i + 1 %>
      <p>From <%= @booking.start_date.strftime("%d/%m/%Y") %> until <%= @booking.end_date.strftime("%d/%m/%Y") %> (<%= pluralize(length, "day") %>)</p>
      <p>Owner contact details: <%= @booking.cow.user.email %></p>
    </div>
  </ul>
  <%= link_to "Back to all cows",  cows_path, class: "btn btn-flat" %>
</div>
